name: Deploy to Firebase Hosting (IIT-ISM)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        env:
          # Firebase Config
          VITE_FIREBASE_AUTH_DOMAIN: collegecentralfake.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: collegecentralfake
          VITE_FIREBASE_STORAGE_BUCKET: collegecentralfake.firebasestorage.app
          # Firebase Secrets
          VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}
          # Domain Config
          VITE_ALLOWED_EMAIL_DOMAIN: "@iitism.ac.in"
          VITE_HOSTED_DOMAIN: "iitism.ac.in"
          # External Services
          VITE_GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # Shared Services
          VITE_EMAILJS_SERVICE_ID: ${{ secrets.EMAILJS_SERVICE_ID }}
          VITE_EMAILJS_TEMPLATE_ID: ${{ secrets.EMAILJS_TEMPLATE_ID }}
          VITE_EMAILJS_PUBLIC_KEY: ${{ secrets.EMAILJS_PUBLIC_KEY }}
        run: npm run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: collegecentralfake
          channelId: live
